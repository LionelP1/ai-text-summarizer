<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Summarizer</title>
</head>
<body>
    <% if (error) { %>
        <div class="error-message">
            <p><%= error %></p>
        </div>
    <% } %>
    <div class="container">
        <div class="input-section">
            <h1>AI Text Summarizer</h1>
            <form action="/api/summarize" method="POST">
                <label for="text">Enter Text to Summarize:</label>
                <textarea id="text" name="text" rows="15" cols="40" placeholder="Paste your text here..."></textarea>
                <br>
                <button type="submit">Summarize</button>
            </form>
        </div>
        <div class="output-section">
            <% if (summary) { %>
                <h2>Summary:</h2>
                <p><%= summary %></p>
            <% } else { %>
                <h2>Your summary will appear here</h2>
            <% } %>
        </div>
    </div>
</body>
</html>

